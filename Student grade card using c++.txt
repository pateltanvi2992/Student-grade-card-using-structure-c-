//Student grade card using c++ //Tanviben Patel#include <iostream>#include <fstream>#include <iomanip>#include <string>using namespace std;//structure definestruct studentType{   string studentFName;   string studentLName;   int testScore;   char grade;};//Function prototypevoid StudentData(ifstream& inFile, studentType student[]);void studentGrade(studentType student[]);void studentGrade(studentType student[]);void printStudentData(studentType student[]);int main() {   struct studentType student[20];  ifstream inFile;  //ofstream outFile;  inFile.open("student.txt");  //outFile.open("student.txt");  StudentData(inFile, student);  studentGrade(student);  printStudentData(student);  inFile.close();  //outFile.close();  return 0;}void StudentData(ifstream& inFile, studentType student[]){  for (int i=0; i<20; i++)  {    inFile>>student[i].studentLName>>student[i].studentFName>>student[i].testScore;  }}void studentGrade(studentType student[]){  for(int i=0;i<20;i++)  //If score greater than 90 and less or equal to 100 - grade A  if (student[i].testScore<=100 && student[i].testScore>90)    student[i].grade='A';  //If score greater than 80 and less or equal to 90 - grade B  else if (student[i].testScore<=90 && student[i].testScore>80)    student[i].grade='B';  //If score greater than 70 and less or equal to 80 - grade C  else if (student[i].testScore<=80 && student[i].testScore>70)    student[i].grade='C';  //If score greater than 60 and less or equal to 70 - grade D  else if(student[i].testScore<=70 && student[i].testScore>60)    student[i].grade='D';  //between 0 and 60 - grade F    else if(student[i].testScore<=60 && student[i].testScore>0)    student[i].grade='F';  //any other input is wrong  else    cout<< "Wrong test score entered ";     }int highestScore(const studentType student[]){     //Declare variables     int high=0,i;     for(i=0;i<20;i++)     //If high value is less than score     if (high < student[i].testScore )     //Set score as high     high=student[i].testScore;     //Return highest score of the students     return high;}void printStudentData(studentType student[]){  cout<<endl;  cout << "*******"<<"Students Data Table"<<"**********"<<endl;  cout<<std::setw(13)<<"Last Name"<<setw(15)<<"First Name"<<setw(15)<<"TestScore"<<right<<setw(7)<<"Grade"<<endl;  for (int i=0; i<20; i++)  {    cout <<setw(10)<<student[i].studentLName<<", "<<setw(15)<<student[i].studentFName<<", "<<setw(13)<< student[i].testScore <<", "<<setw(5)<<student[i].grade<<", "<<endl;  }  int high;  high = highestScore(student);  cout << "highest score is :- "<< high <<endl;  cout<<"Students who having the highest test score: "<<endl;  for (int i=0; i < 20; i++)  {    //If score is high    if (student[i].testScore==high )     //Display student name who have highest score    {      cout<<student[i].studentLName<<", "<<student[i].studentFName<< endl;    }  }}